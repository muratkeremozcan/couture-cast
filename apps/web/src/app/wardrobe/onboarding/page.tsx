// Story 4.4 Task 5 owner: guided wardrobe onboarding route. Thin by design: Next.js's
// generated route types require this file to export nothing but the whitelisted route
// exports (`default`, `metadata`, ...), so the actual implementation lives in
// `../../components/wardrobe-onboarding-flow.tsx` — see that file's header comment for why.
'use client'

import { WardrobeOnboardingFlow } from '../../components/wardrobe-onboarding-flow'

export default function WardrobeOnboardingPage() {
  return <WardrobeOnboardingFlow />
}
