<!-- markdownlint-disable MD013 MD036 MD052 -->

# Story 5.1 draft review log

Adversarial review of the CC-5.1 story draft, run before any implementation
work, mirroring the three-layer blind review that Story 4.4 used
(`_bmad-output/project-knowledge/learning-path-step-by-step.md` Step 32).

**Date:** 2026-08-10
**Artifact reviewed:** `5-1-affiliate-shop-this-look-cta.md`, first draft
**Method:** three reviewers run concurrently, each blind to the others' output
and to this log.

| Reviewer | Mandate                                                                                                                        | Inputs                             | Findings |
| -------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------- | -------- |
| R1       | Zero project context. Read the story file and nothing else. Flag anything an implementer could not act on without guessing.    | The story file only                | 36       |
| R2       | Full project context. Verify every technical claim against the real codebase; find missing integration points.                 | Story file plus the codebase       | 38       |
| R3       | Requirements audit. Line-by-line against `epics.md` CC-5.1, `prd.md`, `sprint-status.yaml`. Report drift, creep, and omission. | Story file plus planning artifacts | 35       |

**Outcome:** 109 findings. 96 fixed, 13 skipped as trivial or as pre-existing
repo convention outside this story's remit. Each skip carries its reason below.

---

## Convergent findings

Independently reported by two or more blinded reviewers. Per the Step 32 lesson,
these were treated as ground truth and reproduced against the codebase before
acting.

| #   | Finding                                                                                                                                                                                                                                  | Reviewers                                                                                                       | Resolution                                                                                                                                                                                                                                                                                                    |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Up to three offers, one CTA control, no defined mapping from tap to `offer_id`; and no constraint keeping the offers to a single partner, so "with partner ID" and a single `partner_id` analytics property are undefined.               | R1 #2, R3 A-2, A-3                                                                                              | Cut to **exactly one offer, one partner, one control** (decision 3). The three-offer cap appeared nowhere in `epics.md` or `prd.md`. Removing it also deleted R1 #8, #9 and R3 B-3.                                                                                                                           |
| C2  | AC 1's positive path is unreachable: inactive seed partner, flag defaulting false, no catalog write path, no `authenticated` access to catalog tables, yet Task 9 demanded E2E via "public-API setup".                                   | R1 #5, R3 A-4, B-10, C-8                                                                                        | New decision 14, "Making the feature reachable": active seeded partner and offers, seeded flag row, test-only secret, all behind `allowsTestOnlySecrets()`. E2E now uses seeded catalog plus public-API **user** setup. Operator runbook added for real partners.                                             |
| C3  | The teen suppression rule was unrequested product invention, contradicted the PRD's 13+ audience and first-named persona, and its technical basis was wrong: `apiRole` comes from Supabase `app_metadata` and is never derived from age. | R3 B-1, D-1 (product); R2 M4 (factual)                                                                          | Reframed as **Open decision 1**, explicitly needing product sign-off, with a stated default and a one-predicate reversal path. Rebased on `hasReachedAgeOfMajority(birthdate)` (verified at `guardian.service.ts:257`), with the null-birthdate case answered explicitly. Story status now records the block. |
| C4  | The `COMMERCE_*` error codes cannot appear on the wire.                                                                                                                                                                                  | R1 #14 (codes undefined), R2 B2 (`common.ts:24-78` schemas are `.strict()` over `{statusCode, message, error}`) | Verified. Decision 9 replaces codes with status plus six exported message constants, and adds the evaluation precedence both reviewers asked for (R1 #13).                                                                                                                                                    |
| C5  | The k6 "adds no more than 50 ms" budget is unmeasurable and invented; the harness has no baseline-diff facility.                                                                                                                         | R1 #25, R2 N30, R3 B-4                                                                                          | Replaced with an absolute P95 SLO key in both branches of `k6/helpers/config.ts:9-45` plus a `scenarioNames` entry.                                                                                                                                                                                           |
| C6  | "Catalog-write-time validation" refers to code the story explicitly defers; and returning `500` to a user who already tapped contradicts the PRD's neutral-card fallback.                                                                | R1 #19, R3 D-3                                                                                                  | Decision 7 drops the phantom write-time hook, keeps per-request validation, states the `500` behaviour plainly, and logs the missing PRD neutral-fallback and weekly-validation job in `deferred-work.md` with citations.                                                                                     |
| C7  | Locale catalogs unenumerated and "reviewed translations" named a review process that does not exist, for compliance copy.                                                                                                                | R1 #21, R2 N25, R3 B-12                                                                                         | Decision 16 enumerates all ten, names the two new parity specs, flags the `APPROVED_COGNATES` collision risk and the `en-US.json` first-sibling-namespace fact, and makes disclosure translations explicit drafts pending human review before the story closes.                                               |
| C8  | `affiliate_cta_shown` has no payload identity to dedupe on; "per ritual payload" is not a key.                                                                                                                                           | R1 #22, R2 N27                                                                                                  | `recommendation_id` (the `ScenarioOutfit.id`) is now the dedupe key. It simultaneously resolves R3 D-2 by supplying the impression-to-click correlation the PRD's 6% metric needs.                                                                                                                            |
| C9  | Manual VoiceOver/TalkBack runs are assigned to CC-3.8 by name in `epics.md`.                                                                                                                                                             | R1 #35, R3 B-5                                                                                                  | Scoped down to a one-time check of the new controls, with the CC-3.8 ownership noted in the task.                                                                                                                                                                                                             |

---

## R1, zero project context

| #   | Finding                                                                                                           | Disposition                                                                                                                                                                                                                                        |
| --- | ----------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `shopThisLookSchema` mandated but never defined.                                                                  | **Fixed.** Full field-by-field schema in decision 11, with `.strict()` and per-field descriptions.                                                                                                                                                 |
| 2   | Multi-offer vs single control.                                                                                    | **Fixed** — C1.                                                                                                                                                                                                                                    |
| 3   | Conversion row identity contradictory: per-event unique key vs per-click mutation semantics.                      | **Fixed.** `AffiliateConversion` is now **append-only** (decision 8). No update path, so last-write-wins, ordering, tie-breaks and forbidden transitions all vanish. Also resolves #31.                                                            |
| 4   | Click request body, `scenario`/`surface` domains, and success status all unspecified.                             | **Fixed.** Decision 7 gives the body, makes `surface` a closed enum, derives `scenario`/`localeRegion` server-side, and specifies `201`/`200`.                                                                                                     |
| 5   | No way to construct an eligible test state.                                                                       | **Fixed** — C2.                                                                                                                                                                                                                                    |
| 6   | `affiliate_cta_shown` client-emitted but required a server HMAC and a `TelemetryEvent` row.                       | **Fixed.** Now client-side PostHog only, using the mobile client's own `distinctId` like `trackMobileRitualCreated`. AC 4 corrected to scope its assertions to the two server events.                                                              |
| 7   | Per-user feature flag cannot be evaluated on a webhook with no user.                                              | **Fixed.** The kill switch no longer applies to the webhook at all (decision 8), which also resolves #26.                                                                                                                                          |
| 8   | "At most three" reads as a gating predicate in one place and a truncation cap in another.                         | **Fixed** — moot under C1.                                                                                                                                                                                                                         |
| 9   | Three selection rules with no precedence between them.                                                            | **Fixed** — explicit `ORDER BY (comfort_range IS NULL) ASC, priority DESC, id ASC LIMIT 1`, with "exactness beats priority" stated.                                                                                                                |
| 10  | Three mutually exclusive statements about when `affiliate_cta_clicked` is emitted; "telemetry claim" undefined.   | **Fixed.** Decision 12 states it once: after commit, fail-open, no claim row, no rollback-on-telemetry-failure. The undefined term is gone.                                                                                                        |
| 11  | `.nullable().optional()` recreates the absent-vs-null ambiguity the design bans.                                  | **Fixed.** Now `.nullable()` only; the key is always serialized.                                                                                                                                                                                   |
| 12  | Three Prisma enums mandated with no members or casing.                                                            | **Fixed.** Decision 10 declares all three with lowercase members matching their JSON one-to-one, so no mapping layer is needed.                                                                                                                    |
| 13  | Status-code precedence undefined on both endpoints.                                                               | **Fixed** — C4.                                                                                                                                                                                                                                    |
| 14  | Webhook error codes and success body undefined.                                                                   | **Fixed** — C4; success body is `{ data: { received: true } }`.                                                                                                                                                                                    |
| 15  | Missing/malformed headers and unresolvable secrets unhandled.                                                     | **Fixed.** Decision 8 step 1 and 2 cover both, all returning an identical `401` so the endpoint is not an enumeration oracle.                                                                                                                      |
| 16  | `webhook_secret_ref` is an unbounded `process.env[<db value>]` read.                                              | **Fixed.** Constrained by a check constraint and a runtime guard to `^COMMERCE_PARTNER_[A-Z0-9_]{1,40}_WEBHOOK_SECRET$`. Genuine security finding.                                                                                                 |
| 17  | Dedupe boundary undefined; no mechanism prevents a concurrent double-insert that a required test asserts against. | **Fixed.** Strictly-greater 60s boundary, database clock, and a partial unique index plus conflict-retry.                                                                                                                                          |
| 18  | "Re-verify the full eligibility chain" is impossible for condition 4 from the click payload.                      | **Fixed.** Decision 7 and Task 4 scope re-verification to conditions 1 to 3 plus offer active-and-in-window, and say why the outfit is not re-derived.                                                                                             |
| 19  | Catalog-write-time validation has no caller; `allowed_host` matching semantics undefined.                         | **Fixed** — C6, plus explicit host rules: exact or dot-suffix, lowercased, post-IDN via `new URL().hostname`, no userinfo, and a missing-placeholder rejection.                                                                                    |
| 20  | User deletion cascade-deletes clicks, which the same story calls commercial records.                              | **Fixed.** `AffiliateConversion.affiliate_click_id` is `onDelete: SetNull`, so the financial fact survives erasure without the personal link. Asserted in AC 4.                                                                                    |
| 21  | Locales unenumerated; "reviewed translations" undefined.                                                          | **Fixed** — C7.                                                                                                                                                                                                                                    |
| 22  | No payload identity for the impression dedupe.                                                                    | **Fixed** — C8.                                                                                                                                                                                                                                    |
| 23  | Matched-case conversion `distinctId` undefined.                                                                   | **Fixed.** Decision 12 states both cases.                                                                                                                                                                                                          |
| 24  | Web auth, token source, and signed-out rendering unspecified.                                                     | **Fixed.** Decision 17 names `createWebApiClient` plus the `sessionStorage` bearer, and specifies the signed-out disabled state — required, because the axe suite loads `/settings` with no session.                                               |
| 25  | k6 budget has no computable baseline.                                                                             | **Fixed** — C5.                                                                                                                                                                                                                                    |
| 26  | Webhook `503` contradicts the story's own retry-storm reasoning.                                                  | **Fixed** — the webhook always records.                                                                                                                                                                                                            |
| 27  | `locale_region` has no wildcard, no fallback, no no-locale default, and mixes ISO with UN M.49.                   | **Fixed.** Decision 4 adds the `'*'` sentinel, the no-locale default, a check constraint, and states plainly that this is a UI-language region and not a jurisdiction.                                                                             |
| 28  | `AuditLog.event_type` type unknown; no-op `PUT` response undefined; insert shape incomplete.                      | **Fixed.** Decision 13 confirms it is a plain `String` (`schema.prisma:629`), gives the full insert shape, and specifies `200` with current state and no row on a no-op.                                                                           |
| 29  | No id strategy; `AffiliateClick.id` is security-relevant as a public URL parameter and orders the tie-break.      | **Fixed.** Decision 10 sets `cuid()` for row ids and adds a separate HMAC `token` column for the URL — see also R2 M19.                                                                                                                            |
| 30  | Effective-window inclusivity, null `effective_to`, and clock source undefined.                                    | **Fixed.** From inclusive, to exclusive, null is open-ended, database clock in the same query.                                                                                                                                                     |
| 31  | Equal-`occurredAt` tie-break and forbidden transitions undefined.                                                 | **Fixed** — moot under the append-only model (#3).                                                                                                                                                                                                 |
| 32  | `loading` and `opensInBrowser` have no placement rule.                                                            | **Fixed.** Decision 16 says both render visibly and gives the accessible-name composition order.                                                                                                                                                   |
| 33  | `apps/mobile/src/lib/` may not be a real convention; Maestro filename is a literal placeholder.                   | **Half fixed.** Maestro flow now named `maestro/commerce-affiliate.yaml`. The path half is **skipped as incorrect**: `apps/mobile/src/lib/` exists and holds `settings-storage.ts`, `user.ts`, `wardrobe.ts`. Expected of a zero-context reviewer. |
| 34  | Kill switch says nothing about the preferences endpoints.                                                         | **Fixed.** Decision 9 states they are ungated and the settings section renders unconditionally.                                                                                                                                                    |
| 35  | Manual VoiceOver/TalkBack is human-only with prose pass criteria.                                                 | **Fixed** — C9.                                                                                                                                                                                                                                    |
| 36  | Browser-handoff failure after a successful mint is unhandled.                                                     | **Fixed.** Decision 7 states the click row and event stand, the localized error shows, and no compensating event is emitted.                                                                                                                       |

---

## R2, full project context

Every claim below was reproduced against the worktree before acting.

| #   | Finding                                                                                                                                                                                                   | Disposition                                                                                                                                                                                                                   |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | `rawBody: true` on `src/main.ts` alone leaves the webhook broken everywhere it actually runs: `apps/api/api/index.ts:12-14` is the deployed entry per `vercel.json`, and test bootstraps take no options. | **Fixed.** Verified both files and `vercel.json`. Decision 8 and Task 5 name all three bootstraps. Highest-value finding in the review: the draft would have shipped a webhook that verified locally and 401'd in production. |
| B2  | Named error codes have nowhere to live in the `.strict()` error envelope.                                                                                                                                 | **Fixed** — C4. Verified `common.ts:24-78`.                                                                                                                                                                                   |
| B3  | "After the cache read" is not a point that exists in `RitualService`: `ritual.service.ts:1060` returns 560 lines before assembly, and `targetComfortRange` is not in scope on the warm path.              | **Fixed.** Decision 5 moves assembly to `RitualController` (line 46-55), and decision 4 restricts placeholder slots to wildcard offers so `targetComfortRange` is never needed.                                               |
| M4  | `teen` is not an age marker; the cited emancipation evidence does not support the claim.                                                                                                                  | **Fixed** — C3.                                                                                                                                                                                                               |
| M5  | `packages/api-client/src/index.ts` does not `export *`, and `apps/api/src/contracts/http.ts` is a hand-maintained re-export block every controller imports through.                                       | **Fixed.** Decision 11 mandates the deep subpath for clients (matching existing precedent) and adds `apps/api/src/contracts/http.ts` to the file list.                                                                        |
| M6  | Three analytics registries, one guarded by a lockstep equality test.                                                                                                                                      | **Fixed.** Decision 12 and Task 2 name all three.                                                                                                                                                                             |
| M7  | `TelemetryService` has a hard-coded two-event ternary and disjunction; `buildAnalyticsSubjectId` is module-private.                                                                                       | **Fixed.** Decision 12 requires generalizing to a set plus a builder table and exporting the helper.                                                                                                                          |
| M8  | "Writes nothing" is already false: `ApiExceptionFilter` persists a telemetry row for every `HttpException`.                                                                                               | **Fixed.** AC 2 rescoped to commerce tables, and the webhook route is excluded from `api_error_occurred` capture, since an unauthenticated endpoint writing a row per rejected request is an amplification vector.            |
| M9  | `PersonalizationModule` imports neither `FeatureFlagsModule` nor `TelemetryModule`.                                                                                                                       | **Fixed.** Task 3 states the wiring and the direction, to avoid a cycle.                                                                                                                                                      |
| M10 | Touching `RitualService`'s constructor breaks twelve positional construction sites.                                                                                                                       | **Fixed** — avoided entirely by B3's controller-level assembly.                                                                                                                                                               |
| M11 | The suites carrying AC 4 and AC 5 evidence do not run in CI: `packages/db` has no `test:coverage`, no workflow runs `test:integration`, and mobile E2E is `workflow_dispatch` only.                       | **Fixed.** Task 1 adds `test:coverage` to `packages/db`; Task 10 states the remaining gaps explicitly rather than implying coverage. Verified `packages/db/package.json`.                                                     |
| M12 | The mobile device cache defeats AC 3's immediacy promise: 15 minutes online, indefinitely offline.                                                                                                        | **Fixed.** New decision 6 requires stripping `shopThisLook` before `saveRitualCache` and never rendering from cache. Verified `index.tsx:196-220`.                                                                            |
| M13 | Replacing the `/settings` stub breaks `accessibility-hardening.spec.ts` unless four attributes are preserved, and that suite loads the route unauthenticated.                                             | **Fixed.** Decision 17 names all four, plus the signed-out requirement. Verified `primaryRoutes` and the auth early-return.                                                                                                   |
| M14 | Web has two competing auth conventions and no stated signed-out behaviour.                                                                                                                                | **Fixed** — see R1 #24.                                                                                                                                                                                                       |
| M15 | `apps/web/vitest.setup.ts:15-22` throws on unmocked `/api/` requests; `mockRitualResponse` has no `shopThisLook`.                                                                                         | **Fixed.** Both handler registries added to Tasks 6 and 7 and to the file list.                                                                                                                                               |
| M16 | Coverage ratchets gate CI and nine new API files are in scope.                                                                                                                                            | **Fixed.** Task 10 lists all three threshold sets and requires unit coverage of error branches, since the integration suites do not run in CI.                                                                                |
| M17 | `locale_region` conflates UI language with commerce jurisdiction.                                                                                                                                         | **Fixed** — decision 4 states the semantics plainly and logs the limitation.                                                                                                                                                  |
| M18 | The claim "nothing can be copied" is false: `wardrobe-upload-token.ts:8-46` is exactly the pattern, including the `timingSafeEqual` length hazard and the test-secret path.                               | **Fixed.** Decision 8 now opens by pointing at both precedents. Removing the invented-from-scratch framing also gave the webhook tests a way to run without a real secret.                                                    |
| M19 | A cuid row id used as a public URL parameter and the sole join key, combined with "unknown token returns 200", lets any partner-secret holder attribute revenue to guessed ids.                           | **Fixed.** Separate HMAC `token` column, unique-indexed; the row id stays internal. Genuine security finding.                                                                                                                 |
| N20 | Contract specs live flat in `packages/api-client/testing/`, not a `__tests__` directory.                                                                                                                  | **Fixed.** Verified the directory.                                                                                                                                                                                            |
| N21 | Any `.refine()` fails `contract-invariants-documented.spec.ts` without an `.openapi()` description.                                                                                                       | **Fixed.** Decision 11 states the rule and prefers structural constraints.                                                                                                                                                    |
| N22 | `packages/db/prisma/seed.ts` does not exist; the real entry is `seeds/index.ts`.                                                                                                                          | **Fixed.** Verified. Decision 14 names the real files and the `faker.seed(4242)` determinism anchor.                                                                                                                          |
| N23 | `rls-policies.spec.ts` has hard-coded table arrays and asserts an exact policy-name set.                                                                                                                  | **Fixed.** Decision 10 and Task 1 name the array and the four exact policy names, and point at line 631 as the no-grant template.                                                                                             |
| N24 | Cleanup cannot scope the three non-user tables with `buildUserFilter`.                                                                                                                                    | **Fixed.** Task 2 requires the `cleanupScopeStartedAt` anchor and gives the delete order.                                                                                                                                     |
| N25 | All four locale parity specs are subtree-scoped; a new tree is audited by nothing.                                                                                                                        | **Fixed** — C7.                                                                                                                                                                                                               |
| N26 | `Linking.openURL` appears nowhere; the pattern is `WebBrowser.openBrowserAsync`, which is an in-app browser, making the copy inaccurate.                                                                  | **Fixed.** Verified zero hits repo-wide. Decision 7 switches to `openBrowserAsync` and the string became "Opens in an in-app browser".                                                                                        |
| N27 | There is no StrictMode in this app; the real hazard is the cache-served payload.                                                                                                                          | **Fixed.** Task 6 drops the StrictMode language, points at the `useRef` precedent, and keys the guard on `recommendation_id`.                                                                                                 |
| N28 | `app/**` is not in the mobile vitest include; and an existing all-locales overflow assertion is at risk from the new strings.                                                                             | **Fixed.** Task 6 gives the real test paths and adds the overflow assertion.                                                                                                                                                  |
| N29 | Pact work spans five files and two registries; `pact/` was absent from the file list.                                                                                                                     | **Fixed.** All five named in Task 8 and the file list, with the provider-state naming convention.                                                                                                                             |
| N30 | k6 has two registries and no baseline-diff facility.                                                                                                                                                      | **Fixed** — C5.                                                                                                                                                                                                               |
| N31 | `scripts/run-maestro.mjs:56` defaults to two flows; a new flow would never run under the listed commands.                                                                                                 | **Fixed.** Task 9 adds per-flow scripts and the verification list uses them.                                                                                                                                                  |
| N32 | There is no global auth wiring to exclude the webhook from.                                                                                                                                               | **Fixed.** Decision 8 replaces the defensive phrasing with a concrete instruction plus a supertest assertion.                                                                                                                 |
| N33 | `.js` on relative imports is not a repo-wide rule.                                                                                                                                                        | **Fixed.** Restated as the `src/modules/` convention, with the `app.module.ts` counter-example noted.                                                                                                                         |
| N34 | Per-handler `@Header` never applies when the service throws; the middleware form is the only correct one for an error-heavy story.                                                                        | **Fixed.** Decision 11 mandates `CommerceCacheHeadersMiddleware`.                                                                                                                                                             |
| N35 | The deferred-defect citation was wrong on both line and field, and the entry is deep-link reachable.                                                                                                      | **Fixed.** Corrected to `lookbook-prism-layout.tsx:41-47`, the `eyebrow` field, with the `deep-link-handler.ts:39` reachability noted — which strengthens the case for logging it.                                            |
| N36 | `npm run test --workspace api` already includes `integration/`.                                                                                                                                           | **Fixed.** Verification block annotated with the service prerequisites.                                                                                                                                                       |
| N37 | Feature-flag registration touches four files, including the seed that changes checked-in database state.                                                                                                  | **Fixed.** Decision 13 names all four; the seed one became the mechanism for C2.                                                                                                                                              |
| N38 | `verify:changed` maps only `packages/` and `apps/`; five touched directories are unverified by it.                                                                                                        | **Fixed.** Task 10 states the limitation.                                                                                                                                                                                     |

---

## R3, requirements audit

| #             | Finding                                                                                                                                          | Disposition                                                                                                                                                                                                                                                                             |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A-1           | Surface narrowing to Mobile against an unqualified "eligible cards", recorded as locked rather than escalated.                                   | **Fixed.** Decision 2 keeps the narrowing, which the codebase forces, but states the evidence and drops the locked framing. The Web CTA absence and the outfit-detail-view surface are both logged in `deferred-work.md`.                                                               |
| A-2, A-3      | "with partner ID" and "button" are singular; mapping undefined.                                                                                  | **Fixed** — C1.                                                                                                                                                                                                                                                                         |
| A-4           | AC 1 not demonstrable in any provisioned environment.                                                                                            | **Fixed** — C2.                                                                                                                                                                                                                                                                         |
| A-5           | "Eligible" redefined to exclude the PRD's primary persona.                                                                                       | **Fixed** — C3.                                                                                                                                                                                                                                                                         |
| A-6           | AC 2 is covered by roughly 40x its own text, all marked non-negotiable.                                                                          | **Fixed structurally.** The "locked, do not re-litigate" framing is gone from the header; a source-vs-derived traceability table now opens the story. The volume itself stays: an HMAC contract cannot be specified in one sentence.                                                    |
| A-7, E-3      | PRD NFR Security 4 requires third-party **disclosure** in settings, not only a toggle.                                                           | **Fixed.** Decision 15 adds a settings disclosure paragraph and a `commerce.settings.disclosure` string naming what is shared and with whom. AC 3 asserts it.                                                                                                                           |
| B-1           | Teen exclusion is unrequested invention with product-wide consequence.                                                                           | **Fixed** — C3.                                                                                                                                                                                                                                                                         |
| B-2           | The Web settings page is Epic 3 work, justified partly by test-harness convenience.                                                              | **Partly fixed.** The page stays, because this repo ships cross-surface controls for cross-surface features. The test-convenience justification is **removed**, and decision 2 now states that AC 3 is satisfiable by the Mobile toggle alone and Task 7 drops whole if review cuts it. |
| B-3           | Offer ranking engine is invention; "three" appears in no source.                                                                                 | **Fixed** — C1 removed the cap. The remaining ranking is the minimum needed to pick one row deterministically.                                                                                                                                                                          |
| B-4           | 50 ms budget, 500-offer catalog and EXPLAIN evidence fabricated then made blocking.                                                              | **Fixed** — C5.                                                                                                                                                                                                                                                                         |
| B-5           | `epics.md` assigns manual VoiceOver/TalkBack to CC-3.8 by name.                                                                                  | **Fixed** — C9.                                                                                                                                                                                                                                                                         |
| B-6           | Kill switch unrequested and elevated to an AC.                                                                                                   | **Kept, now justified.** The traceability table marks AC 6 derived and states why: an operator-managed catalog with no admin console has no other safe degradation path.                                                                                                                |
| B-7           | "The PRD requires ... auditable" inserts a word into a quoted requirement.                                                                       | **Fixed.** Decision 13 now says plainly that this is not a PRD requirement and that keeping the audit row is a local choice. Accurate citation matters more than the outcome.                                                                                                           |
| B-8           | Retention carve-out is a self-generated AC for a self-introduced hazard.                                                                         | **Kept, and completed.** Also answers D-6: a concrete 24-month policy and a pruner, rather than removing the tables from retention entirely.                                                                                                                                            |
| B-9           | RLS matrix, Pact, and factories are repo convention, not story requirements.                                                                     | **Skipped as correct-but-not-actionable.** Recorded by the reviewer for completeness. These conventions are how this repo ships; dropping them would be the deviation.                                                                                                                  |
| B-10          | An inactive seed partner cannot exercise the flow it claims to enable.                                                                           | **Fixed** — C2.                                                                                                                                                                                                                                                                         |
| B-11          | The epic's user story was rewritten with two added requirements.                                                                                 | **Fixed.** Restored verbatim from `epics.md`.                                                                                                                                                                                                                                           |
| B-12          | Ten locales exceeds the PRD's three-language commitment.                                                                                         | **Fixed.** Decision 16 notes the PRD commits to three and ten is repo reality that the parity tests require.                                                                                                                                                                            |
| B-13          | `rawBody: true` is an app-wide bootstrap change from a commerce story.                                                                           | **Kept, scope widened correctly.** Necessary, and R2 B1 showed it must touch three bootstraps, not one. Task 5 requires confirming no route regresses.                                                                                                                                  |
| B-14          | Contract version bump unrequested.                                                                                                               | **Skipped as repo convention.** `architecture.md:185-187` makes `info.version` the record of contract change.                                                                                                                                                                           |
| C-1           | Stated prerequisites verified complete.                                                                                                          | No action.                                                                                                                                                                                                                                                                              |
| C-2           | Baselined on a story recorded `in-progress`.                                                                                                     | **Fixed as a correction.** The reviewer could not see that 4.4's code is merged and present at the baseline commit; only the `sprint-status.yaml` rows on `origin/main` are stale, pending an unpushed commit. Stated in the story.                                                     |
| C-3 to C-7    | Undeclared dependencies on CC-4.1, CC-4.2, CC-4.3, CC-3.2, CC-3.8, CC-0.7, CC-0.9, CC-0.11, CC-0.14, CC-3.6.                                     | **Fixed.** A Prerequisites paragraph now states that the epic's list is incomplete for this codebase and enumerates the real set, each verified `done`.                                                                                                                                 |
| C-8           | Undeclared operational dependency: no owner for provisioning partners, secrets, or the flag.                                                     | **Fixed.** Decision 14 adds an operator runbook and requires naming the owner in the PR description.                                                                                                                                                                                    |
| D-1           | Minor exclusion contradicts the PRD's audience.                                                                                                  | **Fixed** — C3.                                                                                                                                                                                                                                                                         |
| D-2           | The privacy hardening makes the PRD's ≥6% commerce-conversion metric uncomputable: no session or correlation key on any event.                   | **Fixed** — C8's `recommendation_id` supplies the join. Decision 12 documents the tradeoff explicitly rather than leaving it silent. Sharp finding: privacy work quietly defeating the metric the story exists to move.                                                                 |
| D-3, E-1, E-2 | PRD NFR Integration 2 requires weekly link validation and a neutral-card fallback; the story has neither and surfaces a `500` to a user instead. | **Partly fixed, partly deferred.** Both gaps are now named in the out-of-scope list with the PRD citation and logged in `deferred-work.md`. Building a validation job is a separate story; hiding the gap was the defect.                                                               |
| D-4           | Opt-out-by-default versus the PRD's "opt-in/out" phrasing.                                                                                       | **Fixed as a documented tension.** Epic AC 3 says "opt-out toggle" and takes priority; decision 15 states the tension and logs it.                                                                                                                                                      |
| D-5           | Disclosure timing correct.                                                                                                                       | No action.                                                                                                                                                                                                                                                                              |
| D-6           | No retention policy for tables exempted from pruning.                                                                                            | **Fixed** — 24 months plus a pruner, asserted in AC 4.                                                                                                                                                                                                                                  |
| E-4           | The story cites the UX spec's sponsorship-label contract, then defers the undisclosed hardcoded entry.                                           | **Kept as deferred, better documented.** N35 corrected the citation and added the deep-link reachability. Rewriting placeholder copy inside a commerce story would imply a sponsorship that does not exist.                                                                             |
| E-5           | PRD's outfit-detail-view click-to-buy surface never addressed.                                                                                   | **Fixed.** Now an explicit out-of-scope entry: the surface does not exist in the codebase. Logged.                                                                                                                                                                                      |
| E-6           | PRD NFR Localization 2's per-locale currency presentation unaddressed.                                                                           | **Skipped as moot.** No price is shown anywhere: `shopThisLookSchema` carries no price field and analytics forbid it. Conversion currency is stored server-side in minor units and never rendered. Revisit when prices surface.                                                         |
| E-7           | `epics.md` "Key files" says `services/commerce`; the repo uses NestJS modules.                                                                   | **Skipped.** The epic's `services/*` names are notional across every epic; the repo layout is authoritative.                                                                                                                                                                            |
| F-1           | Three epic ACs became seven story ACs, four with no epic counterpart, so test IDs trace to the story and never to the epic.                      | **Fixed.** A two-part traceability table now opens the story: epic AC to story AC, then every story AC marked source or derived with a one-line reason.                                                                                                                                 |
| F-2           | Orphan tasks against the epic's ACs.                                                                                                             | **Fixed by the same table**, which makes each derived AC's justification explicit and auditable rather than implicit.                                                                                                                                                                   |
| F-3           | No epic AC is orphaned; the failure direction is one-way.                                                                                        | No action.                                                                                                                                                                                                                                                                              |

---

## Skipped findings and reasons

Thirteen findings were not acted on. Each reason is recorded so a reviewer can
overrule it.

| #                    | Finding                                                                     | Reason for skipping                                                                                                                                                                                                                |
| -------------------- | --------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1 #33 (path half)   | `apps/mobile/src/lib/` may not be a convention.                             | Factually incorrect. The directory exists with three sibling modules. The Maestro half of the same finding was fixed.                                                                                                              |
| R3 B-9               | RLS matrix, Pact, and factory conventions are not story requirements.       | Correct but not actionable. These are how this repo ships every user-scoped table; omitting them would be the deviation, not the compliance.                                                                                       |
| R3 B-14              | Contract version bump unrequested.                                          | Repo convention with an explicit architecture mandate (`architecture.md:185-187`).                                                                                                                                                 |
| R3 E-6               | Per-locale currency presentation.                                           | Moot: no price is rendered on any surface in this story.                                                                                                                                                                           |
| R3 E-7               | `services/commerce` naming.                                                 | The epic's service names are notional; the NestJS module layout is authoritative.                                                                                                                                                  |
| R3 D-5, C-1, F-3     | Three "no drift found" entries.                                             | Confirmations, not findings.                                                                                                                                                                                                       |
| R1 #35 (partially)   | Manual screen-reader evidence is human-only.                                | Irreducible. Scoped down per C9, but a manual check cannot be automated away, and this repo requires it for new controls.                                                                                                          |
| R3 A-6 (volume half) | AC 2 covered by 40x its own text.                                           | The framing was fixed; the volume stays. An HMAC verification contract, an idempotency model, and an attribution scheme cannot be specified in one sentence, and under-specifying them is what the review process exists to catch. |
| R3 B-6, B-8, B-13    | Kill switch, retention carve-out, and the bootstrap change are unrequested. | All three retained as necessary, now with their derivation stated in the traceability table rather than asserted.                                                                                                                  |

---

## Net effect on the draft

- **Removed:** the three-offer cap and its ranking complexity, the
  `.nullable().optional()` ambiguity, the invented `COMMERCE_*` wire codes, the
  webhook kill switch, the conversion mutation model, the telemetry-claim
  rollback, the StrictMode guard, the 50 ms delta budget, the "nothing can be
  copied" framing, and the "locked, do not re-litigate" framing throughout.
- **Added:** the full `shopThisLookSchema`, the click request contract and
  status codes, decision 6 (device cache), decision 9 (error messages and
  precedence), decision 14 (making the feature reachable), the settings
  disclosure, the HMAC click token, the append-only conversion model, the
  24-month retention policy, the real prerequisite set, the traceability table,
  and roughly 25 previously unnamed integration points.
- **Escalated rather than decided:** the minors question, now Open decision 1
  and the story's one stated blocker.
