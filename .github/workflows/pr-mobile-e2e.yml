# Step 12 step 6 owner: searchable owner anchor
name: Mobile e2e (Maestro)

# This workflow runs the SAME harness as a local run: `scripts/run-maestro.mjs`
# against Expo Go, with the local API and Metro started by the script itself.
#
# It used to do something different, and that divergence is why it never caught
# anything. The old job built a debug APK, installed it as `com.couturecast.app`
# and invoked `npx maestro test` directly with `MOBILE_E2E_SKIP_SERVER=1`. That
# meant no API, no Metro, no seeded fixture user and, decisively, no `APP_URL`,
# which every flow dereferences in `openLink: ${APP_URL}`. Only three shallow
# flows were ever listed and two of them were `continue-on-error`, so the job
# could report success while the app under test never even mounted. A crash that
# took down all eighteen flows for months (`Intl.Segmenter` constructed at module
# scope, absent in Hermes) sailed through it untouched.
#
# Two consequences worth knowing before editing:
#
# 1. Expo Go replaces the Gradle build. `scripts/install-expo-go.mjs` downloads
#    the Expo Go APK and installs it over adb, so `expo prebuild` and
#    `./gradlew assembleDebug` are gone, and with them the Android build-tools
#    and platform SDK packages that only that build needed. `MAESTRO_APP_ID` is
#    deliberately not set here: `run-maestro.mjs` resolves it to
#    `host.exp.exponent` for Android.
# 2. Hardware acceleration is on. The job has always enabled KVM and then
#    thrown it away by passing `-accel off`, forcing software emulation and the
#    30-40 minute cold runs the old comment complained about. GitHub extended
#    hardware-accelerated Android virtualization to standard Linux runners in
#    April 2024 and doubled public-repo runners to 4 vCPU / 16 GB in January
#    2024, so the flag is simply left at the action's `auto` default now.
# Triggers match the Playwright workflow's convention exactly: every pull
# request, every push to `main`, plus manual dispatch. They were dispatch-only
# while the suite was being proven, because an automatic run would have spent an
# hour producing a red result already known about. That condition is met: the
# suite reached a verified 18/18 both in CI and locally, so it now gates PRs.
#
# `workflow_dispatch` stays, as the optional path for running a subset by hand.
# Its `suite` input is dispatch-only by nature: on a `pull_request` or `push`
# event the `inputs` context is empty, so `${{ inputs.suite }}` renders as an
# empty string, the smoke comparison in "Resolve this shard's flow list" is
# false, and discovery takes the full-suite branch. That is the intended
# behaviour for an automatic run; there is no default to declare here for it.
on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      suite:
        description: Which flows to run
        required: true
        default: full
        type: choice
        options:
          - full
          - smoke

concurrency:
  group: mobile-e2e-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Read-only by default. The reporting job at the bottom widens its own
# permissions to what a PR comment needs, rather than granting write to the four
# emulator jobs that only ever read.
permissions:
  contents: read

env:
  # Identical to LOCAL_E2E_DATABASE_URL in scripts/local-e2e-database.mjs and to
  # the Playwright workflow's LOCAL_DATABASE_URL. Both point at the Supabase
  # container declared in supabase/config.toml (port 54322).
  LOCAL_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
  # Not 30. API 30 emulators on GitHub-hosted runners have had no network
  # connectivity since 2026-06-25, which for this suite is a total-failure mode
  # rather than a slowdown: the device has to reach Metro, the local API and
  # Supabase on the host. Expo Go fails the manifest fetch about 0.7 seconds
  # after the deep link, shows its own "Something went wrong." screen, and every
  # flow is then reported as `tab-home` never appearing, which reads as the app
  # being broken. The reporter's own resolution was to bump the API level.
  # https://github.com/ReactiveCircus/android-emulator-runner/issues/479
  #
  # API 30 also carries a long-standing SystemUI "isn't responding" dialog on
  # this action that holds window focus for a whole run, which is directly in
  # the way of an out-of-process UI driver.
  # https://github.com/ReactiveCircus/android-emulator-runner/issues/140
  #
  # Bumping the level changes the AVD cache key, so the next run pays one cold
  # snapshot generation by design.
  ANDROID_API_LEVEL: '34'
  ANDROID_TARGET: google_apis
  ANDROID_ARCH: x86_64
  # `medium_phone`, matching what `scripts/run-maestro-shards.mjs` creates
  # locally. This is a correctness requirement, not a preference.
  #
  # A `pixel_3a` is 1080x2220 at 440dpi, which is about 807dp tall. A
  # `medium_phone` is 1080x2400 at 420dpi, about 914dp. CI was therefore running
  # a screen roughly 12% shorter than the one every local run used, and this
  # suite asserts on elements inside scrolling sections. The result was a class
  # of failure that reproduced nowhere else: `premium-status-line`,
  # `garment-confirm-image`, `reorder-row-.*` and `garment-tag-comfort-mild` all
  # reported "not visible" in CI while passing locally, because
  # `assertVisible`/`extendedWaitUntil` require an element inside the VIEWPORT
  # rather than merely in the hierarchy, and a shorter screen pushes more of
  # each section below the fold.
  #
  # Two devices means two layouts and a suite that cannot tell a real regression
  # from a screen-height artifact. Flows should still scroll to what they assert
  # on, and several now do, but that is defence in depth rather than a reason to
  # keep the asymmetry.
  #
  # This changes the AVD cache key, so the next run pays one cold snapshot
  # generation, exactly as the API-level comment above describes.
  ANDROID_PROFILE: medium_phone

jobs:
  mobile-e2e-android:
    runs-on: ubuntu-latest
    # Sharded across parallel jobs, because serial does not fit.
    #
    # Measured on a clean run (job 96438220797 of run 32373171812): each shard
    # pays about 11 minutes of fixed cost before its first flow assertion runs
    # -- Node/Android SDK setup, the AVD cache restore, Supabase, the database
    # reset, and the emulator boot plus Expo/Metro warm-up inside the "Run
    # Maestro suite" step itself -- on top of its own slice of flow time. Wall
    # clock is that fixed cost plus the slowest shard's slice, so the shard
    # count is chosen to keep the slowest slice under about 4 minutes against
    # `scripts/maestro-flow-durations.json`'s recorded flow costs, landing the
    # whole gate under 15 minutes with some margin.
    #
    # Each shard is a full, independent job: its own emulator, Supabase, API and
    # Metro, running a slice of the flow list. They share the AVD snapshot cache,
    # so the expensive part is paid once per key rather than once per shard.
    #
    # `scripts/resolve-maestro-shard.mjs` assigns flows by recorded DURATION
    # (longest-processing-time-first bin packing), not by count or alphabetical
    # round robin. A count-balanced split still lets one shard draw a
    # disproportionately expensive slice -- flow cost here ranges from well
    # under a minute to several minutes -- and whichever shard draws it becomes
    # the job's floor while the others sit idle. See that script for the
    # algorithm and `scripts/maestro-flow-durations.json` for the recorded
    # baseline it packs against.
    strategy:
      # A failing shard must not cancel the others. The point of a suite is to
      # learn everything that is broken in one run.
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4, 5, 6, 7]
    timeout-minutes: 45
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node environment
        uses: ./.github/actions/install
        with:
          install-command: npm ci

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # No explicit system-image install step: android-emulator-runner's own SDK
      # installer runs `sdkmanager --install "system-images;android-<api>;<target>;<arch>"`
      # from the same inputs, so a step here only duplicated it and gave the two
      # a chance to drift apart.

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      # Restore and save are deliberately split rather than using the combined
      # `actions/cache@v4`. That action declares `post-if: success()`, so its
      # save step is skipped the moment any earlier step fails, and a failing
      # Maestro flow is exactly the case this job exists to produce. Combined,
      # the cache would therefore never be written on a red run, and every red
      # run would pay a full cold boot: the optimisation would cancel itself
      # out at precisely the times it is most needed. `save-always` is the
      # documented-but-deprecated patch for this; the restore/save split is
      # what the action's own docs point to instead.
      - name: Restore cached AVD and snapshot
        uses: actions/cache/restore@v4
        id: avd-cache
        with:
          path: |
            ~/.android/avd/*
            ~/.android/adb*
          # The runner image is part of the key on purpose. A snapshot is only
          # valid for the emulator binary and system image it was captured with,
          # and this job reinstalls the SDK from the runner image every time. On
          # a key without it, an emulator bump on the image silently invalidates
          # the snapshot while the key still HITS, so every later run restores a
          # snapshot it cannot load and pays a full cold boot with nothing in the
          # log to say why. That is the failure this cache exists to avoid.
          key: avd-${{ runner.os }}-${{ env.ImageOS }}-${{ env.ANDROID_API_LEVEL }}-${{ env.ANDROID_TARGET }}-${{ env.ANDROID_ARCH }}-${{ env.ANDROID_PROFILE }}

      # First run for a given key boots a cold emulator and saves a snapshot.
      # Every later run restores it, which is the whole point of the cache: the
      # old job passed `-no-snapshot` and so paid a full cold boot every time.
      - name: Create AVD and generate snapshot for caching
        if: steps.avd-cache.outputs.cache-hit != 'true'
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: ${{ env.ANDROID_API_LEVEL }}
          target: ${{ env.ANDROID_TARGET }}
          arch: ${{ env.ANDROID_ARCH }}
          profile: ${{ env.ANDROID_PROFILE }}
          ram-size: 2048M
          disk-size: 4096M
          force-avd-creation: false
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          emulator-boot-timeout: 900
          disable-animations: false
          script: echo "Generated AVD snapshot for caching."

      # Saved here, immediately after generation, rather than at job end. The
      # snapshot is complete the moment the step above shuts its emulator down,
      # so caching it does not need to wait on the test result that follows, nor
      # be forfeited by it.
      #
      # No `always()`: a bare `if:` is implicitly `success() && (...)`, which is
      # exactly right. Forcing the save to run regardless would publish a
      # half-written AVD under the primary key whenever generation itself
      # failed, and a poisoned cache is worse than no cache because every later
      # run restores it instead of rebuilding.
      - name: Save AVD and snapshot to cache
        if: steps.avd-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.android/avd/*
            ~/.android/adb*
          key: ${{ steps.avd-cache.outputs.cache-primary-key }}

      - name: Start local Supabase services
        uses: ./.github/actions/start-local-supabase

      - name: Reset local database
        run: npm run db:reset
        env:
          DATABASE_URL: ${{ env.LOCAL_DATABASE_URL }}

      # `maestro:install` pipes get.maestro.mobile.dev into bash, and that
      # installer only advertises itself by appending to ~/.bashrc, ~/.zshrc and
      # ~/.bash_profile. Actions runs every step as `bash --noprofile --norc`,
      # which sources none of them, so the binary lands in ~/.maestro/bin and
      # stays invisible. That failure is silent rather than loud: run-maestro.mjs
      # catches the ENOENT and falls back to `npx --yes maestro@latest`, so the
      # job would keep working while testing against a floating Maestro version
      # that no developer runs locally. Export the path, then assert the binary
      # really is the one being used.
      #
      # Reproduced in a Linux container: with `bash --noprofile --norc` the
      # freshly installed binary is not on PATH, and appending ~/.maestro/bin
      # resolves it. The installer also needs a JDK and `unzip` present. Both
      # ship on the ubuntu runner image, and if either goes missing the
      # installer exits non-zero and fails this step loudly rather than
      # degrading to the npx fallback.
      - name: Install Maestro CLI
        run: |
          npm run maestro:install --silent
          echo "$HOME/.maestro/bin" >> "$GITHUB_PATH"

      - name: Verify Maestro CLI is on PATH and is the pinned version
        run: |
          set -euo pipefail
          if ! command -v maestro >/dev/null 2>&1; then
            echo "::error::maestro is not on PATH after install. run-maestro.mjs would silently fall back to 'npx maestro@latest' and test a different version than local runs use."
            exit 1
          fi
          echo "Using maestro at: $(command -v maestro)"
          maestro --version
          # Presence was never the real risk. This step used to accept any
          # version, so CI ran whatever get.maestro.mobile.dev served that day
          # while developers ran whatever Homebrew had, and nothing reported the
          # gap. scripts/maestro-install.mjs owns the pin; this asserts it held.
          # Imported, not scraped. This used to regex the constant out of
          # `scripts/maestro-install.mjs`, which broke the moment the pin moved
          # into its own module: the match returned null and the step died on
          # `[1] of null` — a failure that says nothing about Maestro. Asking the
          # module for its own value cannot drift from what the installer uses.
          PINNED=$(node -e "import('./scripts/maestro-version.mjs').then((m) => console.log(m.MAESTRO_PINNED_VERSION))")
          if [ -z "$PINNED" ]; then
            echo "::error::Could not read the pinned Maestro version from scripts/maestro-version.mjs."
            exit 1
          fi
          FOUND=$(maestro --version 2>/dev/null | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1)
          if [ "$FOUND" != "$PINNED" ]; then
            echo "::error::maestro $FOUND is on PATH but this repo pins $PINNED. CI and local would be testing different versions."
            exit 1
          fi
          echo "Maestro version matches the pin: $PINNED"

      - name: Resolve this shard's flow list
        id: flows
        run: |
          set -euo pipefail
          node ./scripts/resolve-maestro-shard.mjs \
            --suite "${{ inputs.suite }}" \
            --shard ${{ matrix.shard }} \
            --shards ${{ strategy.job-total }} \
            >> "$GITHUB_OUTPUT"

      - name: Run Maestro suite
        if: steps.flows.outputs.empty != 'true'
        uses: reactivecircus/android-emulator-runner@v2
        env:
          DATABASE_URL: ${{ env.LOCAL_DATABASE_URL }}
          MOBILE_E2E_DATABASE_URL: ${{ env.LOCAL_DATABASE_URL }}
          MOBILE_E2E_PLATFORM: android
          # The action supplies the emulator, so the runner must not try to boot
          # a second one through scripts/start-android-emulator.sh.
          MOBILE_E2E_AUTO_BOOT_ANDROID: '0'
          # run-maestro.mjs shims `xcrun` onto PATH for Android runs, to stop
          # Maestro hanging on a broken CoreSimulator install. That is a macOS
          # developer-machine workaround and it is worse than useless here:
          # Linux has no /usr/bin/xcrun for the shim to delegate to, and putting
          # an `xcrun` on PATH where the platform has none inverts the very
          # signal Maestro uses to decide whether iOS tooling exists. The script
          # provides this escape hatch for exactly this case.
          MAESTRO_DISABLE_ANDROID_XCRUN_SHIM: '1'
          GUARDIAN_INVITE_JWT_SECRET: ${{ secrets.GUARDIAN_INVITE_JWT_SECRET }}
        with:
          api-level: ${{ env.ANDROID_API_LEVEL }}
          target: ${{ env.ANDROID_TARGET }}
          arch: ${{ env.ANDROID_ARCH }}
          profile: ${{ env.ANDROID_PROFILE }}
          # `ram-size` and `disk-size` are deliberately absent here, unlike in
          # the generation step above. The action applies them by appending
          # `hw.ramSize` and `disk.dataPartition.size` to the AVD's config.ini,
          # and this step runs against a config.ini restored from cache that
          # already carries those lines from creation. Appending them a second
          # time changes the hardware configuration the snapshot was captured
          # against, so the emulator rejects it:
          #
          #   ERROR | The emulator hardware cannot load snapshot: default_boot
          #   USER_INFO | The emulator is starting from scratch.
          #               Reason: different AVD configuration
          #
          # The cache was therefore paying 25 seconds to restore a snapshot that
          # was never loaded, and every run still took a ~40 second cold boot.
          # The values themselves are not lost by omitting them: they live in
          # the cached config.ini, which is the whole point of caching it.
          force-avd-creation: false
          emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          # This boot restores the snapshot the previous step saved, so it is
          # short. A long ceiling here only buys a longer hang before the job
          # gives up, and it competes with `timeout-minutes` for the same budget.
          emulator-boot-timeout: 600
          disable-animations: true
          # Several flows type into text fields (wardrobe onboarding, capsule
          # create). The Android spell checker's suggestion strip is a known
          # source of flake around text entry, and nothing under test depends
          # on it.
          disable-spellchecker: true
          # One line, deliberately, invoking a real bash script.
          #
          # This action splits `script:` on newlines and runs EACH LINE through
          # its own `sh -c`, and `/usr/bin/sh` here is dash. So a multi-line
          # block is not the shell script it appears to be: `set -euo pipefail`
          # dies on line one (which cost this job a full run), a bare `set -eu`
          # is inert for every later line, and variables, `cd` and multi-line
          # constructs do not survive between lines. Keeping the body in
          # scripts/ci-mobile-e2e.sh gets real bash and working strict mode, and
          # lets a developer run the exact CI path locally.
          script: bash ./scripts/ci-mobile-e2e.sh ${{ steps.flows.outputs.list }}

      - uses: actions/upload-artifact@v4
        if: always() && steps.flows.outputs.empty != 'true'
        with:
          # Per shard, and per attempt. Four jobs uploading one name collide,
          # and upload-artifact v4 rejects the duplicate rather than merging, so
          # the shard that lost the race would have no artifacts at all -- and
          # artifacts are how this suite is diagnosed.
          #
          # The attempt is in the name for the same reason. A v4 artifact
          # belongs to the RUN, not to the attempt, and it outlives the attempt
          # that produced it, so re-running a failed shard uploads into a name
          # that already exists and gets a 409 -- losing the artifacts of the
          # run you re-ran precisely because you needed to look at them. The
          # alternative, `overwrite: true`, resolves the conflict by deleting
          # the earlier attempt's evidence, which is the wrong half to keep.
          name: android-test-results-shard-${{ matrix.shard }}-attempt-${{ github.run_attempt }}
          path: maestro/artifacts/
          retention-days: 7

  # One report for the whole matrix, in the same shape every other test tier in
  # this repo posts: Playwright, Pact, k6 and the unit suite all leave a sticky
  # PR comment. This suite left only a red or green check across four jobs, so
  # finding out WHICH flow broke meant downloading four artifacts, and in
  # practice a red shard got read as "mobile is flaky again" and re-run.
  #
  # A separate job rather than a step in the matrix, because a per-shard comment
  # would be four comments racing for the same marker, and because the shard
  # jobs run inside an emulator action whose `script:` is a poor place to do
  # anything that talks to the GitHub API.
  report:
    needs: mobile-e2e-android
    # `always()`, or the report disappears exactly when it is wanted: a default
    # `if` skips this job when any shard fails.
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      # Reading the run's artifacts to build the download links.
      actions: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Summarize the Maestro run and comment
        uses: ./.github/actions/mobile-e2e-report-comment
        with:
          # Scoped to this attempt. Artifacts belong to the run rather than the
          # attempt, so a re-run leaves the previous attempt's uploads in place
          # and an unscoped pattern would report both, double-counting flows and
          # producing two "shard 1" download links.
          artifact-pattern: android-test-results-shard-*-attempt-${{ github.run_attempt }}
          suite-result: ${{ needs.mobile-e2e-android.result }}
          # Keep in step with the `shard:` matrix above. `strategy.job-total` is
          # not readable from a job that only `needs:` the matrix, so this is a
          # label rather than a derived value; the flow counts in the comment
          # come from the reports themselves and are unaffected by it.
          shards: '7'
          platform: Android (Expo Go)
          device-label: API ${{ env.ANDROID_API_LEVEL }} · ${{ env.ANDROID_PROFILE }} · ${{ env.ANDROID_ARCH }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
