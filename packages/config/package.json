{
  "name": "@couture/config",
  "version": "0.0.1",
  "private": true,
  "description": "Shared configuration contracts for CoutureCast",
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint --max-warnings=0 --ext .ts src",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  }
}
