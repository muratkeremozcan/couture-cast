{
  "name": "@couture/api-client",
  "version": "0.0.1",
  "private": true,
  "description": "Shared API client types and schemas",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint --max-warnings=0 --ext .ts src scripts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node -e \"console.log('@couture/api-client: no unit tests configured')\"",
    "gen:openapi:events": "tsx scripts/generate-events-openapi.ts"
  },
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "^7.3.4",
    "ts-node": "^10.9.2",
    "zod": "^3.25.76"
  }
}
