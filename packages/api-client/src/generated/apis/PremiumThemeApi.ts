/* eslint-disable */
/**
 * CoutureCast HTTP API
 * Canonical HTTP contracts shared across API, web, mobile, and tests.
 *
 * The version of the OpenAPI document: 1.4.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime'
import type {
  PremiumThemeResponse,
  UpdatePremiumThemeInput,
  UpdatePremiumThemeResponse,
} from '../models/index'

export interface ApiV1CommercePremiumThemePutRequest {
  updatePremiumThemeInput: UpdatePremiumThemeInput
}

/**
 *
 */
export class PremiumThemeApi extends runtime.BaseAPI {
  /**
   * Creates request options for apiV1CommercePremiumThemeGet without sending the request
   */
  async apiV1CommercePremiumThemeGetRequestOpts(): Promise<runtime.RequestOpts> {
    const queryParameters: any = {}

    const headerParameters: runtime.HTTPHeaders = {}

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('bearerAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/api/v1/commerce/premium/theme`

    return {
      path: urlPath,
      method: 'GET',
      headers: headerParameters,
      query: queryParameters,
    }
  }

  /**
   * Returns the palette the acting user should render, together with the entitlement and flag state that produced it. Deliberately NOT entitlement-gated: every signed-in caller needs an answer, and a non-entitled caller simply reads theme null. A stored key the server no longer recognises also resolves to null rather than failing, so a stale client value degrades to Default instead of breaking the settings page.
   * Read the resolved premium theme
   */
  async apiV1CommercePremiumThemeGetRaw(
    initOverrides?: RequestInit | runtime.InitOverrideFunction
  ): Promise<runtime.ApiResponse<PremiumThemeResponse>> {
    const requestOptions = await this.apiV1CommercePremiumThemeGetRequestOpts()
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response)
  }

  /**
   * Returns the palette the acting user should render, together with the entitlement and flag state that produced it. Deliberately NOT entitlement-gated: every signed-in caller needs an answer, and a non-entitled caller simply reads theme null. A stored key the server no longer recognises also resolves to null rather than failing, so a stale client value degrades to Default instead of breaking the settings page.
   * Read the resolved premium theme
   */
  async apiV1CommercePremiumThemeGet(
    initOverrides?: RequestInit | runtime.InitOverrideFunction
  ): Promise<PremiumThemeResponse> {
    const response = await this.apiV1CommercePremiumThemeGetRaw(initOverrides)
    return await response.value()
  }

  /**
   * Creates request options for apiV1CommercePremiumThemePut without sending the request
   */
  async apiV1CommercePremiumThemePutRequestOpts(
    requestParameters: ApiV1CommercePremiumThemePutRequest
  ): Promise<runtime.RequestOpts> {
    if (requestParameters['updatePremiumThemeInput'] == null) {
      throw new runtime.RequiredError(
        'updatePremiumThemeInput',
        'Required parameter "updatePremiumThemeInput" was null or undefined when calling apiV1CommercePremiumThemePut().'
      )
    }

    const queryParameters: any = {}

    const headerParameters: runtime.HTTPHeaders = {}

    headerParameters['Content-Type'] = 'application/json'

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken
      const tokenString = await token('bearerAuth', [])

      if (tokenString) {
        headerParameters['Authorization'] = `Bearer ${tokenString}`
      }
    }

    let urlPath = `/api/v1/commerce/premium/theme`

    return {
      path: urlPath,
      method: 'PUT',
      headers: headerParameters,
      query: queryParameters,
      body: requestParameters['updatePremiumThemeInput'],
    }
  }

  /**
   * Stores the acting user\'s palette choice and returns the freshly resolved state, the same shape the GET returns. Null resets to Default by upserting the stored row to null; the row is never deleted, not on reset and not on entitlement loss. Status precedence is fixed so assertions are deterministic: 403 (\"A Premium subscription is required for this feature.\") outranks 503 (\"Premium themes are temporarily unavailable.\"), because the entitlement guard runs pre-handler and the flag check lives in the service body. A non-entitled caller therefore never observes the kill switch.
   * Select or reset the premium theme
   */
  async apiV1CommercePremiumThemePutRaw(
    requestParameters: ApiV1CommercePremiumThemePutRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction
  ): Promise<runtime.ApiResponse<UpdatePremiumThemeResponse>> {
    const requestOptions =
      await this.apiV1CommercePremiumThemePutRequestOpts(requestParameters)
    const response = await this.request(requestOptions, initOverrides)

    return new runtime.JSONApiResponse(response)
  }

  /**
   * Stores the acting user\'s palette choice and returns the freshly resolved state, the same shape the GET returns. Null resets to Default by upserting the stored row to null; the row is never deleted, not on reset and not on entitlement loss. Status precedence is fixed so assertions are deterministic: 403 (\"A Premium subscription is required for this feature.\") outranks 503 (\"Premium themes are temporarily unavailable.\"), because the entitlement guard runs pre-handler and the flag check lives in the service body. A non-entitled caller therefore never observes the kill switch.
   * Select or reset the premium theme
   */
  async apiV1CommercePremiumThemePut(
    requestParameters: ApiV1CommercePremiumThemePutRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction
  ): Promise<UpdatePremiumThemeResponse> {
    const response = await this.apiV1CommercePremiumThemePutRaw(
      requestParameters,
      initOverrides
    )
    return await response.value()
  }
}
